Leanring for EF Core 9.0

##Running Migrations
# Add a migration
dotnet ef migrations add InitialCreate --project OM.MFPTracker.Data --startup-project ConsoleApp.Test
dotnet ef migrations add InitBlazor --project OM.MFPTracker.Data --startup-project OM.MFPTracker.Web

# Update the database
dotnet ef database update --project OM.MFPTracker.Data --startup-project ConsoleApp.Test
dotnet ef database update --project OM.MFPTracker.Data --startup-project OM.MFPTracker.Web

##Delete the Database
dotnet ef database drop --project OM.MFPTracker.Data --startup-project ConsoleApp.Test

##Remove the Last Migration
dotnet ef migrations remove --project OM.MFPTracker.Data --startup-project ConsoleApp.Test


##delete the migrations folder & re-initialize
dotnet ef migrations add InitialCreate --project OM.MFPTracker.Data --startup-project ConsoleApp.Test
dotnet ef database update --project OM.MFPTracker.Data --startup-project ConsoleApp.Test

Icon: https://icons.getbootstrap.com/

An alternative option (faster)
Open Package Manager Console (Tools → NuGet Package Manager → Package Manager Console)
select Data project (the project has dbcontext)
setp1: Add-Migration InitialCreate
step2: Update-Database
step3: Drop-Database

useful tsqls
=============
SELECT Id, SchemeName, AmcId FROM TMutualFunds WHERE AmcId NOT IN (SELECT Id FROM TAmc);
SELECT * FROM TAmc
SELECT Id, SchemeName, AmcId FROM TMutualFunds
delete and clean tabel
DELETE FROM [TMutualFundTransaction]
DELETE FROM SQLITE_SEQUENCE WHERE name='TMutualFundTransaction';

MF NAV History
https://www.advisorkhoj.com/mutual-funds-research/historical-NAV/Nippon%20India%20Small%20Cap%20Gr%20Gr?start_date=02-02-2025&end_date=02-02-2026
https://www.indianfundbazaar.com/historical-nav/
https://portal.amfiindia.com/spages/NAVOpen.txt (download current date NAV of all MF)
MF List
https://nsdl.co.in/mutual-fund-popup.htm

8) What I can deliver next (your call)

🔧 Updated OnModelCreating file assembled as one block for your solution.
🧩 Repo implementations for AMC, Fund, Category, Folio, Transactions (with uniqueness guards & AMC match).
🖥️ Pages for AMC / Folio / Transactions aligned to your two‑row filters, small/bold headers, JS confirm delete, JS‑free modals, NOCASE filters.
🗂️ Migration plan with example Up/Down steps to safely evolve from the previous schema.

If you want to start incrementally, I suggest:

Apply AMC + Folio (with AMCId) and adjust Transactions;
Then update Fund repo/UI to respect AMCId;
Finally adjust Transactions page to use the AMC‑safe selectors.

Tell me where you want to begin (e.g., Folio + Fluent + Repo + Page), and I’ll generate the drop‑in code.

If you want next, I can help you with:

✅ Historical NAV backfill (NAVAll.txt)
✅ BackgroundService for daily sync
✅ NAV chart + valuation
✅ Return calculations


Trnsecation
===========
✔ Multiple funds per folio
✔ Switch transactions
✔ Dividends
✔ Tax reports
✔ Capital gain FIFO logic (later)
✔ XIRR calculation
✔ Portfolio performance

improvements
=============
FIFO lot tracking

Capital gains calculation

XIRR engine

NAV price history table

Performance charts

Asset allocation view
End-to-end refactoring (AMC → Folio → Fund → Transactions)
UI consistency pass for all pages
Dashboard summary widgets (Total AMCs, Total Folios, Total Transactions...)
Export to Excel / CSV
Server-side filtering improvements

Errors:
Q1. One or more errors occurred. Failed to launch debug adapter.
A1. Tools → Options → Debugging → General Then uncheck: "Enable JavaScript debugging for ASP.NET (Chrome, Edge)”

Reference: https://groww.in/mutual-funds/category

Remove
=========
Remove all of this (important)
You should now delete / stop using:

MFTAuthStateProviderv0
AddAuthenticationCore
AddAuthorizationCore
AuthorizeView + RedirectToLogin
Manual auth state handling

They are no longer needed.

Change all of this (16-April-26)
Step-by-step plan (so you see the whole path)
We will do exactly these steps, one by one:

Implement LocalAuthService
Add minimal /api/auth/login endpoint
Add /api/auth/logout endpoint
Wire up cookie authentication
Update Login.razor to call the API
Protect pages with [Authorize] again
Optional admin-only authorization

// TODO: Improve returnUrl handling for prerendered Blazor Web App login
// Currently defaults to "/" after login to avoid breaking auth flow

for AI
Start from your original, fully tested code as the base —
apply all changes at once —
and return ONE final, complete file